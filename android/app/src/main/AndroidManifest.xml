<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Needed by image_picker's camera-capture option on the profile photo
         picker — without this declared, some devices refuse the camera
         intent instead of just falling back to gallery-only. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Android 13+ requires this runtime permission before any
         notification (including our event reminders) can be shown at
         all — requested at runtime via NotificationService.requestPermission. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Deliberately NOT requesting SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM:
         reminders are scheduled with AndroidScheduleMode.inexactAllowWhileIdle
         (a few minutes of slack is fine for "don't forget about soccer
         practice"), which needs no special alarm permission. A reboot
         clears any alarms Android had scheduled; NotificationService's
         resync-on-app-open is the mitigation for that, not a boot
         receiver. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:label="family_pulse"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
